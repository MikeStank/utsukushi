// app.js and server.js are connected by $.getJSON("")
// response from server.js is in the form of "data"

// $.getJSON("/all", function(data) {
//
//   console.log(data);
//   for (var i = 0; i < data.length; i++) {
//     $("#results").append("<tr><td>" + data[i].name + "<tr><td>")
//   }
// })
